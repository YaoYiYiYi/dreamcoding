const config = {
  // 启动端口
  port: 3000,

  // 数据库的配置
  database: {
    DATABASE: 'nodesql',
    USERNAME: 'root',
    PASSWORD: '987470yaoyi.',
    POST: '3306',
    HOST: 'localhost'
  }
}
module.exports = config